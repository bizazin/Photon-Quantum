component WeaponId { EWeaponId Value; }
component AbilityCardId { EAbilityCardId Value; }

component BowShotWeapon { }
component OrbitalShotWeapon { }

enum EWeaponId 
{
    None = 0,
    BowShot = 1,
    OrbitalShot = 2,
}

enum EAbilityCardId 
{
    None = 0,
    //Bow Shots
    FireArrow = 101,
    FrostArrow = 102,
    
    //Orbital Shots
    FireBall = 201,
    FrostBall = 202,
}